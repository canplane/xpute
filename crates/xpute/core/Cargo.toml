[package]
name = "xpute-core"
edition.workspace = true
version.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[features]
# The golden records' reader (src/golden.rs), for another crate's tests.
golden = []

[dependencies]
libm.workspace = true

[dev-dependencies]
hex.workspace = true
