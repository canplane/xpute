[package]
name = "xpute-runtime"
edition.workspace = true
version.workspace = true
publish.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
fnv.workspace = true
indexmap.workspace = true
xpute-core = { path = "../core" }

# The vectors' reader, for this crate's own tests.
[dev-dependencies]
xpute-core = { path = "../core", features = ["golden"] }
